// Untyped database stub. supabase-js will treat every row, insert, and update
// as `any`, which is fine until we run `supabase gen types typescript`.
//
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
